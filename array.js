const numbers=[30, 78, 64, 39, 63, 90]
// numbers[0]=20 // it callled set elements methods 

// numbers.push(40) // it (40) will be add on the array list as a last index number .

// numbers.pop() // pop method remove the last element of an array 

console.log(numbers.length) // lenght methods give us the how many elements in this array as i can see in my output there is 6 elements


